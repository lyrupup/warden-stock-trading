import { PlaceholderPage } from "@/components/common/placeholder-page";

/** 持仓分析与风控 /risk（M4） */
export function RiskPage() {
  return <PlaceholderPage title="持仓风控" description="每日 AI 分析 / 风险提示 / 历史报告" milestone="M4" />;
}

/** 明日盘前预案 /risk/premarket（M4） */
export function PremarketPage() {
  return <PlaceholderPage title="盘前预案" description="个股关注价位 / 止盈止损 / 加减仓思路" milestone="M4" />;
}
