import { PlaceholderPage } from "@/components/common/placeholder-page";

/** 策略列表 /strategies（M2） */
export function StrategyListPage() {
  return <PlaceholderPage title="选股策略" description="策略 CRUD / 指标定义 / skill.md / 回测" milestone="M2" />;
}

/** 策略详情 /strategies/:id（M2） */
export function StrategyDetailPage() {
  return <PlaceholderPage title="策略详情" description="基本信息 / 指标 / skill.md / 回测" milestone="M2" />;
}
