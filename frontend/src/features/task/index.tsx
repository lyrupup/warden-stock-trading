import { PlaceholderPage } from "@/components/common/placeholder-page";

/** 定时任务管理 /tasks（M5） */
export function TaskListPage() {
  return <PlaceholderPage title="定时任务" description="任务 CRUD / Cron 配置 / 执行记录 / 飞书推送" milestone="M5" />;
}
