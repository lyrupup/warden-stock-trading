import { PlaceholderPage } from "@/components/common/placeholder-page";

/** 持仓列表 /positions（M3） */
export function PositionListPage() {
  return <PlaceholderPage title="持仓记录" description="账户总览 / 持仓列表 / 行情联动" milestone="M3" />;
}

/** 持仓详情 /positions/:id（M3） */
export function PositionDetailPage() {
  return <PlaceholderPage title="持仓详情" description="交易流水 / 新增交易 / 盈亏重算" milestone="M3" />;
}
